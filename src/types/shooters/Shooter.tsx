export type Shooter = {
    id: number,
    firstName: string,
    lastName: string,
    // fixme change to date instead of age
    // birthDate: Date,
    age: number,
    licence: string,
    address: string,
    sportClub: string
}